# This repo is a heap of somewhat related experiments without a single entry
# point. The tooling, however, demands a standard entry point, so here it it.
#
# grep "function main()" over the src folder for a list of intended entry
# points.
module brow
end
